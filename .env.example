# Base URL of the backend API.
# The Vite dev server proxies /api requests to this address by default
# (see vite.config.ts), so you only need to change this if your backend
# runs on a different host or port.
VITE_API_BASE_URL=http://localhost:8080
